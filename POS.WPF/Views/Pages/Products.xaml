<UserControl x:Class="POS.WPF.Views.Pages.Products"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:layout="clr-namespace:POS.WPF.Views.Layout"
      xmlns:comp="clr-namespace:POS.WPF.Views.Components"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      d:DesignHeight="450" d:DesignWidth="800"
      mc:Ignorable="d">
    <Behaviors:Interaction.Triggers>
    </Behaviors:Interaction.Triggers>
    <Grid Background="White">
        <materialDesign:Snackbar 
            MessageQueue="{Binding MessageQueue}"
            HorizontalAlignment="Right" VerticalAlignment="Top" Panel.ZIndex="1000" Margin="10">
            <materialDesign:SnackbarMessage>
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="CheckBold"/>
                    <TextBlock Text="POS" Margin="10 0"/>
                </StackPanel>
            </materialDesign:SnackbarMessage>
        </materialDesign:Snackbar>

        <materialDesign:Transitioner SelectedIndex="0" AutoApplyTransitionOrigins="True">
            
            <!--Slide #1-->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <layout:HeaderBar Grid.Row="0" DataContext="{Binding GridHeader}"/>
                <materialDesign:DialogHost Grid.Row="1" DialogTheme="Light" Identifier="GridDialog">
                    <comp:ProductList/>
                </materialDesign:DialogHost>
            </Grid>
            
            <materialDesign:TransitionerSlide>
                <materialDesign:TransitionerSlide.BackwardWipe>
                    <materialDesign:CircleWipe/>
                </materialDesign:TransitionerSlide.BackwardWipe>
                <materialDesign:TransitionerSlide.ForwardWipe>
                    <materialDesign:SlideWipe Direction="Right"/>
                </materialDesign:TransitionerSlide.ForwardWipe>
                
                <!--Slide #2-->
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <layout:HeaderBar Grid.Row="0" DataContext="{Binding FormHeader}" Panel.ZIndex="1000"/>
                    <materialDesign:DialogHost Grid.Row="1" DialogTheme="Light" Identifier="FormDialog">
                        <comp:ProductForm/>
                    </materialDesign:DialogHost>
                </Grid>

            </materialDesign:TransitionerSlide>
        </materialDesign:Transitioner>

    </Grid>
</UserControl>

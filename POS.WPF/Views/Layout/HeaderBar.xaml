<UserControl x:Class="POS.WPF.Views.Layout.HeaderBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <materialDesign:ColorZone Mode="Dark" Height="60" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" VerticalContentAlignment="Stretch">
            <TextBlock Text="{Binding HeaderText}" Margin="20 10" VerticalAlignment="Bottom" Foreground="White" FontSize="24" FontWeight="Normal" FontFamily="Aharoni"/>
        </materialDesign:ColorZone>

        <Button HorizontalAlignment="Right" VerticalAlignment="Bottom" 
                Margin="20 -20" Panel.ZIndex="1000" Visibility="{Binding ButtonVisibility}"
                Style="{DynamicResource MaterialDesignFloatingActionButton}" Command="{Binding ButtonCmd}">
            <materialDesign:PackIcon Kind="{Binding IconKind}" Width="30" Height="30"/>
        </Button>
    </Grid>
</UserControl>

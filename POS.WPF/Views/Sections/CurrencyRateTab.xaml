<UserControl
    x:Class="POS.WPF.Views.Sections.CurrencyRateTab"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:system="clr-namespace:System;assembly=mscorlib"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Behaviors:Interaction.Triggers>
        <Behaviors:EventTrigger EventName="SelectionChanged" SourceName="CurrencyDrp">
            <Behaviors:InvokeCommandAction Command="{Binding LoadCurrencyRatesCmd}" PassEventArgsToCommand="True">
                <Behaviors:InvokeCommandAction.CommandParameter>
                    <system:Boolean>True</system:Boolean>
                </Behaviors:InvokeCommandAction.CommandParameter>
            </Behaviors:InvokeCommandAction>
        </Behaviors:EventTrigger>
    </Behaviors:Interaction.Triggers>

    <Grid x:Name="MainGrid">
        <Border Margin="0,10" VerticalAlignment="Top">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <DockPanel
                    Grid.Row="0"
                    Margin="0,0,0,10"
                    LastChildFill="False">
                    <ComboBox
                        x:Name="CurrencyDrp"
                        Width="200"
                        Margin="0,0,10,0"
                        materialDesign:HintAssist.Hint="Currency"
                        materialDesign:HintAssist.IsFloating="True"
                        materialDesign:TextFieldAssist.HasClearButton="True"
                        DisplayMemberPath="Name"
                        DockPanel.Dock="Left"
                        ItemsSource="{Binding CurrencyList}"
                        SelectedItem="{Binding SelectedCurrency}"
                        SelectedValue="{Binding SelectedCurrencyId}"
                        SelectedValuePath="Id" />
                    <Button
                        Margin="0,0,10,0"
                        Command="{Binding DeleteCurrencyRateCmd}"
                        Content="Delete"
                        DockPanel.Dock="Right"
                        Style="{StaticResource MyAccentButton}" />
                    <Button
                        Margin="0,0,10,0"
                        Command="{Binding OpenCurrencyFormCmd}"
                        Content="Add New Rate"
                        DockPanel.Dock="Right" />
                </DockPanel>
                <DataGrid
                    Grid.Row="1"
                    AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    CanUserReorderColumns="False"
                    ItemsSource="{Binding CurrencyRates}"
                    SelectionMode="Single"
                    SelectionUnit="CellOrRowHeader">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn
                            Width="Auto"
                            Binding="{Binding IsChecked}"
                            EditingElementStyle="{StaticResource MaterialDesignDataGridCheckBoxColumnEditingStyle}">
                            <DataGridCheckBoxColumn.HeaderTemplate>
                                <DataTemplate>
                                    <CheckBox Command="{Binding DataContext.CheckAllCurrencyRateCmd, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}}" CommandParameter="{Binding IsChecked, RelativeSource={RelativeSource Self}}" />
                                </DataTemplate>
                            </DataGridCheckBoxColumn.HeaderTemplate>
                        </DataGridCheckBoxColumn>
                        <DataGridTextColumn
                            Binding="{Binding CurrencyName}"
                            Header="Currency"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Binding="{Binding CurrencyCode}"
                            Header="Currency"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Binding="{Binding RateDate, StringFormat='yyyy/MM/dd hh:mm tt'}"
                            Header="Date"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Binding="{Binding BaseValue}"
                            Header="Base Value"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Binding="{Binding Rate}"
                            Header="Rate"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Binding="{Binding FinalRate}"
                            Header="Final Rate"
                            IsReadOnly="True" />
                        <DataGridTextColumn
                            Binding="{Binding Note}"
                            Header="Note"
                            IsReadOnly="True" />
                        <DataGridTemplateColumn Header="Reverse Calc" IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <materialDesign:PackIcon HorizontalAlignment="Center" Kind="{Binding ReverseCalcIcon}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
